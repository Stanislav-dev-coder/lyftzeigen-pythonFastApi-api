version: '3'
services:
  fastapi-app:
      image: emptyvessel/fastapi-app:latest
      build:
        context: .
        dockerfile: ./Dockerfile
      ports:
        - "8081:8081"
      networks:
        - shared-network

networks:
  shared-network:
    external: true